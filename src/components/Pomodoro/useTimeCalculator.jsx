import React from 'react'

const useTimeCalculator = ({response}) => {

  const calculateTime = (response) => {
    if(response) {
      localStorage.setItem('minWork', minWork);
    }
  }


  return {

  }
}

export default useTimeCalculator